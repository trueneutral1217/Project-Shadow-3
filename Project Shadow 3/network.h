#ifndef NETWORK_H
#define NETWORK_H
/*
#include <string>
#include <SDL_net.h>

class Network {
public:
    Network();
    ~Network();
    bool connect(const std::string& host, Uint16 port);
    void disconnect();
    bool send(const std::string& message);
    std::string receive();

private:
    IPaddress ip;
    TCPsocket socket;
};
*/
#endif // NETWORK_H
